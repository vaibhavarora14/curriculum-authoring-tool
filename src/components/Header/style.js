import { COLOR } from "../../constants";

export const header = {
    color: COLOR.GREY,
    textTransform: 'uppercase',
    fontSize: '1.5rem',
    fontWeight: 700,
    borderBottom: `0.1rem solid ${COLOR.GREY}`,
    marginBottom: '0.5rem'
}

export const subHeaderWrapper = {
    display: 'flex',
    paddingBottom: '1rem',
    borderBottom: `0.1rem solid ${COLOR.GREY}`,
}

export const subHeader = {
    color: COLOR.GREY,
    fontWeight: 600
}